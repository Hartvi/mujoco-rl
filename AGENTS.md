First check if you are in a Python environment. Check for example `~/venvs`, `~/venv`, `venv`, `.venv`, conda, or other environments.

## Tests
Run tests using `python -m unittest -q` in this directory.

## pre-commit
Activate the environment (or use the python directly) and `pre-commit run`
